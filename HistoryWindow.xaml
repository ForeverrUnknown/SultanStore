 <Window x:Class="SultanStore.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="История продаж"
        Height="650" Width="1000"
        Background="#121212"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <!-- 🎨 Цвета -->
        <SolidColorBrush x:Key="bg" Color="#121212"/>
        <SolidColorBrush x:Key="card" Color="#1e1e1e"/>
        <SolidColorBrush x:Key="gold" Color="#d4af37"/>

        <!-- 🔥 DataGrid ГЛОБАЛЬНЫЙ СТИЛЬ -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1e1e1e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        
        <!-- 🔥 СКРОЛЛ -->
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="#1e1e1e"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track">

                                <!-- 👇 ВОТ ТУТ Thumb -->
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#d4af37"
                                                CornerRadius="5"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>

                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        
        <!-- ❌ УБИРАЕМ ЛЕВУЮ БЕЛУЮ ПОЛОСУ -->
        <Style TargetType="DataGridRowHeader">
            <Setter Property="Width" Value="0"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

        <!-- 📦 СТРОКИ -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#1e1e1e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>

            <!-- 🔥 УБИРАЕМ БЕЛОЕ ВЫДЕЛЕНИЕ -->
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 📦 ЯЧЕЙКИ -->
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <!-- 📦 ЗАГОЛОВКИ -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#d4af37"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- 🔥 КНОПКИ -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#d4af37"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

    </Window.Resources>

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- 🔥 ВЕРХ -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">

            <Border Width="70" Height="70" CornerRadius="35"
                    Background="#1e1e1e" Padding="5" Margin="0,0,15,0">
                <Image Source="logo1.png" Stretch="Uniform"/>
            </Border>

            <TextBlock Text="История продаж"
                       FontSize="26"
                       Foreground="#d4af37"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 💰 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock x:Name="dayProfit" Foreground="White" Margin="10"/>
            <TextBlock x:Name="weekProfit" Foreground="White" Margin="10"/>
            <TextBlock x:Name="monthProfit" Foreground="White" Margin="10"/>
        </StackPanel>

        <!-- 📅 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <DatePicker x:Name="dateFrom" Width="120" Margin="5"/>
            <DatePicker x:Name="dateTo" Width="120" Margin="5"/>
            <Button Content="Показать" Width="100" Click="FilterByDate"/>
            <Button Content="Неделя" Width="90" Click="LoadWeekChart"/>
            <Button Content="Месяц" Width="90" Click="LoadMonthChart"/>
            <Button Content="Год" Width="80" Click="LoadYearChart"/>
        </StackPanel>

        <!-- 📈 -->
        <lvc:CartesianChart x:Name="salesChart"
                            Grid.Row="3"
                            Margin="0,10,0,0"/>

        <!-- 📦 ТАБЛИЦА -->
        <Border Grid.Row="4"
                Background="#1e1e1e"
                CornerRadius="15"
                Padding="10"
                Margin="0,10,0,0">

            <DataGrid x:Name="gridHistory"
          AutoGenerateColumns="False"
          MouseDoubleClick="gridHistory_MouseDoubleClick">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding SaleID}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Дата продажи"
                                        Binding="{Binding SaleDate}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Сумма"
                                        Binding="{Binding TotalAmount}"
                                        Width="150"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>
</Window>